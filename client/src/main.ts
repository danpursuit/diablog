import "./assets/main.css";

import { createApp } from "vue";
import App from "./App.vue";

const app = createApp(App);
app.mount("#app");
// export default {
//   install(app) {
//     app.directive("drag-and-drop", dragAndDropDirective);
//   },
// };

// app.directive("drag-and-drop", dragAndDropDirective);
